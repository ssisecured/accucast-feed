# Accucast Feed

Static JSON feed for the Accucast iOS app.

- `latest.json`: normalized station, observation, and forecast snapshots.

Station labels in this public feed are anonymized as `Station 1` and `Station 2`.
Locations, coordinates, and original station IDs are intentionally omitted.
